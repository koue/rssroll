	rssroll - rss collector written in c
	version 0.2 of 04 March 2013

rssroll is rss crawler which fetchs rss feeds and stores them into sqlite3 database.
There is web interface for browsing stored feeds which are grouped by category.

Used libraries:

	curl
	libxml2
	sqlite3


Check INSTALL file for setup information.

Used configuration:

	OS: 		FreeBSD 9.0-RELEASE-p3 i386
	Compilator: 	gcc version 4.2.1 20070831 patched [FreeBSD]
	Web server:	thttpd-2.25b

Example:
https://blogroll.chaosophia.net/index.cgi

